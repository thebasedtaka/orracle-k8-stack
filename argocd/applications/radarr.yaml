apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: radarr
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: https://charts.alekc.dev/
      chart: radarr
      targetRevision: 2.3.0
      helm:
        valueFiles:
          - $values/helm/radarr/values.yaml

    - repoURL: https://github.com/thebasedtaka/orracle-k8-stack.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: media

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true